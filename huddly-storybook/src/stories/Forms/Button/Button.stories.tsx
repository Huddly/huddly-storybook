import { Button } from '.';

export default { component: Button };

export const Primary = {
    args: {
        type: 'button',
        label: 'Sample string',
        disabled: false,
        href: '#',
    },
};
