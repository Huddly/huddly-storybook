import { Textarea } from '.';

export default { component: Textarea };

export const Primary = {
    args: {
        label: 'Your message',
        isRequired: true,
        baseHeight: 136,
    },
};
