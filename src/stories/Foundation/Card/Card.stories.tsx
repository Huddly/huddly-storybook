import React from 'react';
import { Card } from '.';

export default { component: Card };

export const LavenderCard = {
  args: {
    children: <div>Text in card</div>,
    background: 'lavender',
  },
};
